<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/registrationView">
    <fragment
        android:id="@+id/registrationView"
        android:name="idd.urbanido.fragments.RegistrationView"
        android:label="RegistrationView" >
        <action
            android:id="@+id/action_registrationView_to_usersView"
            app:destination="@id/usersView" />
    </fragment>
    <fragment
        android:id="@+id/usersView"
        android:name="idd.urbanido.fragments.UsersView"
        android:label="UsersView" />
</navigation>